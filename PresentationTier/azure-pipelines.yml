trigger:
- main

pool:
    vmImage: 'ubuntu-latest'

stages:
- stage: BuildApp
  jobs: 
  - job: BuildPushImage
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: 'MovieApp'
        repository: 'bestmoviesdb.azurecr.io'
        command: 'buildAndPush'
        Dockerfile: '$(Build.SourceDirectory)/MovieServer/Dockerfile'
        tags: '$(Build.BuildId)'